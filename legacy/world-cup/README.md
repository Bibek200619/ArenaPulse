# Archived World Cup demo

Preserved from commit 618c07c. This is the prior standalone Vite/React application, excluded from ArenaPulse production builds. Its sports data and external URLs have not been verified and must not be presented as current live data. Original source and emblem are retained without modification. To inspect it separately, install its own package dependencies and run its Vite scripts from this directory; dependencies use the original historical manifest and are not covered by ArenaPulse's new lockfile or security gates.
