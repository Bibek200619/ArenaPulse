# Ml Integration

The full future integration plan is in plan.md sections 13 and 23. No algorithm has been selected; there is no model, training or inference. Phase 12 adds versioned contracts, registry abstraction, metadata/history/evaluation schema and a disabled API returning model_not_configured with prediction null.
